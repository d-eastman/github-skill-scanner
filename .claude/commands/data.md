---
description: Ori Shapiro (Data Analyst) — define measurable success + instrumentation
argument-hint: [feature, or "define success metrics"]
---

Engage the **data-analyst** subagent (Ori Shapiro).

Request: $ARGUMENTS

Work backwards from the decision the data must inform. Define primary/secondary/guardrail metrics and the minimum instrumentation needed *before* shipping. Produce `docs/dev-team/success-metrics.md` and `docs/dev-team/instrumentation.md`.
