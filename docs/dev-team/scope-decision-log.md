# Scope Decision Log
**Project:** GitHub Skill Scanner  
**Owner:** Sasha Kowalski (Product Manager)  
**Purpose:** Every scope cut (or explicit "this is in") is logged here with the reason and target phase. This is the record that prevents "I thought we agreed" six weeks later.

---

## Log

| # | Date | Item | Decision | Rationale | Target phase |
|---|------|------|----------|-----------|-------------|
| 1 | 2026-06-04 | **Usage analytics / Tier B metrics** (copy-install clicks, searches, page loads) | **OUT of v1** | One-week runway does not accommodate tool selection, integration, and CSP verification. Tier A metrics (free from the metadata envelope) are sufficient to answer "is this working?" post-launch. Ori's recommendation explicitly: fast-follow. Placeholder `console.log('install_copied', ...)` in `CopyButton` is IN v1 — drop-in replacement when Tier B lands. | Fast-follow (1 sprint post-launch) |
| 2 | 2026-06-04 | **Skill detail view / expanded card** (nice-to-have #1) | **OUT of v1** | Requires modal or routing decision, non-trivial UX design, and extra build time. No must-have depends on it. Router introduction (ADR-005) was explicitly rejected for v1. | Phase 2 |
| 3 | 2026-06-04 | **Filter by tag / category** (nice-to-have #2) | **OUT of v1** | Requires schema extension (tags field in SKILL.md frontmatter), which is not in the approved OQ-1 resolution. Tag filter with no tags to filter by is useless. Text search (must-have #7) covers the v1 discovery need. | Phase 2 |
| 4 | 2026-06-04 | **Per-repo JSON files** (nice-to-have #5) | **OUT of v1** | Debug convenience only; no user-facing value. Can be added additively to `data/repos/` without touching the `skills.json` contract (ADR-002 explicitly anticipates this). | Phase 2 (when scan debugging becomes a real need) |
| 5 | 2026-06-04 | **Playwright e2e tests** (E6-S3) | **Should in v1, provisional cut** | High value for regression coverage. First in cut order if Day 5 runs short because manual QA (E6-S1, E6-S2) satisfies the ship gate. If time allows, include. | Fast-follow if cut |
| 6 | 2026-06-04 | **Last-scanned timestamp footer** (nice-to-have #3) | **Should in v1, provisional cut** | Data is free in the envelope (ADR-002); one small component. Include on Day 5 if any time remains. Cut without impact to any must-have. | Phase 2 if cut |
| 7 | 2026-06-04 | **Scan-on-push to `main` trigger** (nice-to-have #4) | **Could in v1, provisional cut** | Low-effort addition to `scan.yml` triggers. Useful during development. Daily cron is sufficient for v1. Cut if DevOps has no spare cycle. | Phase 2 if cut |
| 8 | 2026-06-04 | **`console.log('install_copied', ...)` placeholder in CopyButton** | **IN v1** | Ori's explicit recommendation: costs nothing, confirms the event fires in dev, makes Tier B analytics a drop-in replacement (not a retrofit). Not an analytics tool — just a log line. | v1 |
| 9 | 2026-06-04 | **Launch content gate (E5-S1): confirm repos have SKILL.md files** | **IN v1 as a ship gate** | A technically-passing scan with `skillCount = 0` is not a shipped product. Flagged by BA, Data Analyst, and requirements risk table. `skillCount >= 1` is a pass/fail launch gate (success-metrics.md section 4). This is not a code item — it is an operational check that must happen before go/no-go. | v1 (gate, not code) |
| 10 | 2026-06-04 | **TypeScript across the codebase (frontend + scanner)** | **IN v1** | Stakeholder decision (2026-06-04), overriding the BA's plain-JS assumption. ADR-005 amended accordingly. The ADR-002 data contract is exactly the kind of boundary types protect well; flipping before any code exists is the cheap moment. `npm run typecheck` stays in CI. | v1 |
| 11 | 2026-06-04 | **Dynamic repo discovery** | **OUT — not planned** | Out of scope in requirements. Static hand-maintained config is the v1 model. | Not planned |
| 12 | 2026-06-04 | **Router / state library (React Router, Redux, Zustand)** | **OUT — not planned for v1** | ADR-005 explicitly rejects over-engineering for a single-screen app. If a detail view (nice-to-have #1) lands in Phase 2, prefer a modal over adding a router; only introduce React Router if real multi-route navigation appears. | Phase 2 (conditional) |
| 13 | 2026-06-04 | **Playwright e2e tests (E6-S3)** | **CONFIRMED CUT — fast-follow** | Go/no-go gate reached. Manual QA (E6-S1, E6-S2) satisfied the ship gate. No time remained on Day 5 for Playwright. Becomes TD-003 fast-follow, first sprint post-launch. | Fast-follow |
| 14 | 2026-06-04 | **Last-scanned timestamp footer (N1)** | **CONFIRMED CUT — Phase 2** | Go/no-go gate reached. No time remained on Day 5. Data is in the envelope; component is a one-sprint task. | Phase 2 |
| 15 | 2026-06-04 | **Scan-on-push to main trigger (N2)** | **CONFIRMED CUT — Phase 2** | Go/no-go gate reached. Daily cron is sufficient for v1. | Phase 2 |
| 16 | 2026-06-04 | **SEC-001 fix (gray-matter js engine disabled) + 3 regression tests** | **PULLED INTO v1** | Critical RCE identified in security review. Non-negotiable. Fixed and regression-tested before go/no-go call. Ships in v1. | v1 |
| 17 | 2026-06-04 | **SEC-002 runbook PAT scope correction** | **PULLED INTO v1** | Security review found runbook still referenced `public_repo` scope for classic PATs without the warning against `repo` scope. Doc-only fix; zero code cost; correct before any stakeholder follows the runbook. Ships in v1. | v1 |
| 18 | 2026-06-04 | **SEC-003 fetch size limit on SKILL.md** | **OUT of v1 — TD-008, fast-follow** | Security reviewer finding. Attack surface is limited to maintainers of explicitly configured repos (trusted). Not a blocking risk at current repo count = 1 (anthropics/skills). Fix in first sprint post-launch: 1 MB cap in src/scan/fetcher.ts. | Fast-follow |
| 19 | 2026-06-04 | **SEC-005 / vitest v4 upgrade (CVE GHSA-5xrq-8626-4rwp)** | **OUT of v1 — TD-009, accepted** | Dev-only CVE in vitest UI server; `vitest --ui` is never invoked in CI or production. `npm audit --omit=dev` is clean. Breaking upgrade requires migration guide review. Upgrade in first sprint post-launch, batched with other dependency updates. | Fast-follow |
| 20 | 2026-06-04 | **BUG-001 / TD-007: aria-busy missing on loading container** | **OUT of v1 — a11y fast-follow** | Low severity. Workaround: "Loading skills..." text is present in DOM. One-line fix: `aria-busy={status === 'loading'}` on the catalog section. First a11y sprint post-launch. | Fast-follow |
| 21 | 2026-06-04 | **Go/No-Go: v1** | **GO — conditional** | All must-haves shipped. 61 tests pass. No Critical or High open defects. Content gate cleared (18 real skills). Critical SEC-001 RCE fixed. Condition: stakeholder must complete launch checklist and verify live Pages URL before declaring shipped. See docs/dev-team/go-no-go.md. | v1 |
| 22 | 2026-06-05 | **Scanned repos indicator (SR-1)** — `metadata.repos` field in scanner + header disclosure component | **IN — post-launch fast-follow** | Stakeholder-requested feature; both scoping decisions made by stakeholder (Interpretation B; summary-line + expand). Additive scanner change + small frontend component. Fits the fast-follow sprint. No existing must-have is displaced. Must be sequenced after the uncommitted fast-follow batch (TD-003/007/008/009/010 + install-command change) is committed and PR'd. Nice-to-haves (repo skill counts in expanded list, merged "Last scanned / N repos" footer line) are provisional — UX Designer confirms before implementation; cut without impact if sprint is tight. OQ-SR-3 (Architect to confirm additive-field treatment under ADR-002) must be resolved before scanner implementation begins. | Fast-follow (second batch, same sprint) |
| 23 | 2026-06-05 | **Scan list: 3 repos** (`anthropics/skills`, `vercel-labs/skills`, `juliusbrussee/caveman`) | **CONFIRMED — supersedes anthropics-only launch seed** | Stakeholder-confirmed. The live catalog now includes third-party repos (`vercel-labs/skills`, `juliusbrussee/caveman`) in addition to the Anthropic-owned seed. 26 skills total. The SEC-003 fetch-size risk level is unchanged (all three repos are explicitly configured and operator-trusted). SR-1's `ScannedReposIndicator` surfaces exactly this list to users, which is the right moment to make it visible. | Current (in effect) |
| 24 | 2026-06-05 | **TC-144 — no horizontal scroll at 320px** | **CLOSED** | Was an open must-have test case. Closed by new automated Playwright viewport test (passes). No horizontal scroll at 320px viewport width is confirmed. | Closed |
| 25 | 2026-06-05 | **SR-1 render-guard unit tests (TC-110–113, TC-123)** — loading/error/absent-repos/empty-repos states and empty-catalog+repos-present combo | **OUT of SR-1 merge — fast-follow** | QA-identified gap; all states are correct-by-inspection and covered by the render guard logic. Low risk. Fast unit tests (~30 min). Deferred to keep SR-1 merge clean and not block the commit batch. | Fast-follow (same sprint, after SR-1 merges) |
| 26 | 2026-06-05 | **Go/No-Go: SR-1 (scanned repos indicator)** | **SHIP** | QA clean pass: 90 unit tests pass, 9 e2e tests pass (real Chromium), typecheck clean, `npm audit` 0 vulnerabilities. TC-144 (no horizontal scroll at 320px) closed by new automated test. ADR-002 addendum invariants hold in regenerated live `data/skills.json`. No bugs filed. Remaining gaps (TC-110–113, TC-123 unit tests) are LOW risk, correct-by-inspection, and logged as a fast-follow (entry #25). Uncommitted batch (fast-follows + install-command change) must be committed first; SR-1 merges second. | Fast-follow sprint — SHIPPED |
| 27 | 2026-06-05 | **Modern UI styling (UI-1)** — dark-developer theme, single dark-only palette, CSS token system, full visual refresh | **IN — fast-follow, third batch** | Stakeholder-directed. CSS-only restyle with no DOM/behavior/test-contract changes. Scope is well-bounded: `index.css` token system + palette swap across all surfaces and states. No architecture impact (Architect skipped). WCAG AA contrast is a hard requirement (non-negotiable). System fonts only — no web font, no new network requests. OQ-UI-1 (font stack) is decided by stakeholder: system stacks only. Animation/transitions are explicitly out of scope. Sequences after SR-1 is committed and merged. | Fast-follow (third batch, same sprint) |
| 28 | 2026-06-05 | **Go/No-Go: UI-1 (dark-developer restyle)** | **SHIP** | Automated gate fully clear: typecheck clean, 90/90 unit pass, 12/12 e2e pass, `npm audit` 0 vulnerabilities. WCAG AA contrast hard gate PASS — all 9 pairings computed from actual implemented hex values (e.g. muted text 5.62:1, scan-failed tag 5.14:1, focus ring 7.49:1). `.repo-scan-failed` corrected from #999 to #848d97 to meet the gate. BUG-002 (TC-163 race condition) was a test defect, not a product defect; fixed with one-line `waitFor` addition — e2e suite now 12/12. Visual verification via real browser screenshots confirmed dark theme renders correctly at desktop and 320px (no horizontal overflow). Residual manual cases accepted (see fast-follow list below). Change is CSS-only; no DOM contract, behavior, or accessibility structure was touched. Commit sequencing: SR-1 uncommitted work must be committed first; UI-1 commits on top. | Fast-follow (third batch) — SHIPPED |
| 29 | 2026-06-05 | **GHES support — scanner code + tests (GHE-1 through GHE-5)**: host-parameterized API base, raw content URL, `repoUrl`, dedicated GHES PAT secret, host carried in config | **GO — approved for next increment, scanner code + tests only** | Scope is well-bounded: five must-haves (GHE-1–5) touch `src/scan/` and `tests/` only. Discovery algorithm, data schema, frontend logic, and ADR-001/002 are explicitly unchanged. No frontend code changes are in scope. The work is additive and isolated to the scanner layer; risk of regression to the shipped product is low. Sequencing constraint: SR-1 and UI-1 uncommitted work must be committed and merged before GHES work begins — do not stack a third unmerged branch on top of two. Size estimate: M (scanner parameterization + config shape + PAT routing + test coverage for GHES path); calibrate with lead-developer before sprint start. GHE-N1 (github.com backward compatibility) is a nice-to-have; route to Architect for feasibility before implementing — if it complicates PAT routing, cut it. | Next increment (fourth batch) — after SR-1 + UI-1 are committed |
| 30 | 2026-06-05 | **OQ-GHE-1 — deploy target for GHES-sourced content (public exposure risk)**: should enterprise catalog content (internal repo names, skill names, descriptions, GHES host domain) be published on the existing public GitHub Pages site? | **DEFERRED — explicit stakeholder decision required before any GHES deploy pipeline change** | Stakeholder decision: "build scanner, defer deploy." The scanner code increment (entry #29) proceeds without touching the deploy pipeline. The public GitHub Pages site MUST NOT be pointed at real internal GHES data until this question is resolved. Possible mitigations (internal Pages, auth-gated host, approved-public-only scan list, accept exposure) are documented in requirements-github-enterprise.md Risk 2 — these are options for stakeholder + Architect to evaluate, not requirements. This is a policy and architecture decision, not a scanner code decision. It may be a larger project change than the scanner itself. | Blocked — stakeholder + Architect, no target date |
| 31 | 2026-06-05 | **OQ-GHE-2 — CI network reachability and runner infrastructure**: are GitHub.com-hosted Actions runners able to reach the GHES host? If not, a self-hosted runner inside the corporate network is required before any scheduled scan can exceed. | **DEFERRED — document both paths as open deployment assumptions; confirm before CI/CD design** | Stakeholder decision: "document it." Network reachability from public Azure-hosted runners to a corporate GHES host is UNKNOWN and assumed blocked until confirmed otherwise. If a self-hosted runner is required, that is an infrastructure prerequisite — not a scanner code change — and substantially changes the CI/CD design. The Architect must document both the "publicly reachable" and "self-hosted runner required" paths as open deployment assumptions in the relevant ADR before the CI/CD pipeline is designed for GHES. The scanner code increment (entry #29) is not blocked by this — it does not touch CI/CD. | Blocked — Architect to document; stakeholder to confirm network reachability |
| 32 | 2026-06-05 | **Go/No-Go: GHES scanner code increment (GHE-1–5)** | **SHIP — code increment only; GHES is not live** | All automated gates clear: typecheck clean, 137 unit tests pass (90 prior + 47 new GHES), 12 e2e pass, `npm audit` 0 vulnerabilities. QA verified the security invariant is genuine: 4 no-leak tests (TC-222/223/224/227) assert no cross-host token leakage; tokens are never logged; no hard-coded tokens present; github.com `repoUrl`/schema/behavior is byte-identical to pre-GHES (no regression). One self-corrected brittle test was fixed by the orchestrator; all green. The github.com production deployment is unaffected. What "ship" means here: the code is merged-ready and may be committed as the fourth batch. GHES is not live and cannot be activated until OQ-GHE-1 and OQ-GHE-2 are both resolved (see entries #30 and #31). BUG-003 (Medium: raw `Accept` header path dead code; TC-254 weak assertion) is logged as a fast-follow — it is a test-quality and dead-code issue, not a product outage, but it must be fixed before any live GHES deployment is attempted. Commit sequencing: SR-1 and UI-1 uncommitted batches must be committed and merged first; GHES increment commits fourth, on top of a clean main. | Fourth batch — SHIPPED (code merged-ready); GHES activation blocked on OQ-GHE-1 + OQ-GHE-2 |
| 33 | 2026-06-05 | **BUG-003 — raw `Accept: application/vnd.github.raw` path dead code + TC-254 weak assertion** | **OUT of GHES increment — fast-follow, required before live GHES deployment** | The raw-header path in `client.ts` is never actually sent; the base64 fallback runs instead and produces correct output. No product outage. TC-254 asserts on a branch that never executes. QA flagged: fix before any live GHES deployment. Not blocking the code increment merge (github.com behavior is unaffected), but the fix must land before the GHES activation gate is even considered open. | Fast-follow — required pre-GHES-activation |
